# Champbeam - Backend Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# PostgreSQL
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Validation & Serialization
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# HTTP Client
httpx>=0.26.0

# S3-compatible storage client (used against Supabase Storage's S3 endpoint
# for the file-hosting feature).
boto3>=1.34.0

# MongoDB GridFS storage backend (STORAGE_BACKEND=mongo). gridfs ships with
# pymongo. Only used when the mongo backend is selected.
pymongo>=4.6.0

# QR code generation (SVG output, no Pillow dependency) for short-link QR codes.
qrcode>=7.4

# Redis (caching)
redis>=5.0.0

# Async support
anyio>=4.2.0

# Environment
python-dotenv>=1.0.0

# Email validation
email-validator>=2.0.0

# Rate limiting
slowapi>=0.1.0

# CSV processing (for bulk generator)
pandas>=2.0.0

# Timezone support
pytz>=2024.1

# User agent parsing (for click tracking)
user-agents>=2.2.0

# World map geo visualization (frontend dep listed here for reference)
# react-simple-maps (npm)
# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# In-process SQLite (async) backend used by the pytest suite (see tests/conftest.py).
aiosqlite>=0.19.0
